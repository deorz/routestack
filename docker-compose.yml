services:
  app:
    build:
      context: .
      target: runtime
    environment:
      ROUTESTACK_SERVER_HOST: 0.0.0.0
      ROUTESTACK_DATABASE_URL: sqlite:////data/routestack.db
      ROUTESTACK_SECURITY_SECRET_KEY: change-me-in-production
    ports:
      - "8000:8000"
    volumes:
      - app-data:/data

volumes:
  app-data:
